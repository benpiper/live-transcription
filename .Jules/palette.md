## 2026-05-20 - Empty states in list views
**Learning:** Adding empty states to list components (like watchwords) removes ambiguity and prevents users from interacting with disabled functionality like "Clear All". Explicitly managing disabled states of related buttons (add, clear) reduces confusion.
**Action:** Always include empty states and manage disable conditions for lists when building components that add/remove items dynamically.
